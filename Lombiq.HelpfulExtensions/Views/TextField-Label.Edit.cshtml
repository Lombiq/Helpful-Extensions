@using OrchardCore.ContentFields.Settings
@using OrchardCore.ContentManagement.Metadata.Models
@model OrchardCore.ContentFields.ViewModels.EditTextFieldViewModel

@{
    var settings = Model.PartFieldDefinition.GetSettings<TextFieldSettings>();
}

<div class="ocat-wrapper @Model.PartFieldDefinition" id="@Html.IdFor(x => x.Text)_FieldWrapper">
    <label class="ocat-label">@Model.PartFieldDefinition.DisplayName()</label>
    <div class="ocat-end">
        <input hidden asp-for="Text" />
        <strong>@(Model.Text ?? T["<empty>"].Value)</strong>
        @if (!string.IsNullOrEmpty(settings.Hint))
        {
            <span class="hint">@settings.Hint</span>
        }
    </div>
</div>
